apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: speedwheel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: adamadiaw/speedwheel-frontend:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: speedwheel
spec:
  selector:
    app: frontend
  ports:
    - port: 4200
      targetPort: 80
      nodePort: 30080
  type: NodePort